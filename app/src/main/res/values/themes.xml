<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.Kasa" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/window_background</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:windowLightNavigationBar">true</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
        <item name="android:enforceStatusBarContrast">false</item>
        <item name="android:enforceNavigationBarContrast">false</item>
    </style>

    <!--
        Açılış ekranı teması.

        minSdk 36 olduğu için platformun kendi açılış ekranı nitelikleri
        (API 31+) doğrudan kullanılabiliyor; core-splashscreen uyumluluk
        kitaplığına gerek yok. Simge sistem tarafından, uygulama süreci
        hazırlanırken çiziliyor — yani kullanıcı ana ekranda dokunduğu anda.

        Arka plan simgenin kendi zemini: ana ekrandaki işaretten açılış
        ekranındaki işarete geçiş renk atlamadan oluyor. Bu değer gece/gündüz
        ayrımı yapmıyor, çünkü simgenin kendisi de yapmıyor.
    -->
    <style name="Theme.Kasa.Splash" parent="Theme.Kasa">
        <item name="android:windowSplashScreenBackground">@color/splash_background</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/splash_dial_animated</item>
        <item name="android:windowSplashScreenAnimationDuration">780</item>
    </style>

    <style name="Theme.Kasa.Transparent" parent="Theme.Kasa">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:backgroundDimEnabled">true</item>
    </style>
</resources>
