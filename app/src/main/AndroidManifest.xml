<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Sızıntı denetimi (HIBP k-anonimlik aralık sorgusu) -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- Dokunsal geri bildirim -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- Biyometrik kilit -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <!-- 2FA QR kodu okuma -->
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- Sızıntı ve parola yaşı bildirimleri -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!--
        Bağlama duyarlı kilit süresi: bağlı Wi-Fi ağının adını okumak Android
        10'dan beri konum izni gerektiriyor. Özellik tamamen isteğe bağlı ve
        izin verilmediğinde sessizce devre dışı; ağ adı ham hâlde ne saklanıyor
        ne de cihazdan çıkıyor, yalnızca özeti karşılaştırılıyor.
    -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <uses-feature android:name="android.hardware.wifi" android:required="false" />
    <uses-feature android:name="android.hardware.location" android:required="false" />
    <uses-feature android:name="android.hardware.camera.any" android:required="false" />
    <uses-feature android:name="android.hardware.fingerprint" android:required="false" />

    <!-- Otomatik doldurmada eşleşen uygulamanın simgesini/adını çözebilmek için -->
    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
    </queries>

    <application
        android:name=".KasaApplication"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="false"
        android:hasFragileUserData="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:localeConfig="@xml/locales_config"
        android:networkSecurityConfig="@xml/network_security_config"
        android:enableOnBackInvokedCallback="true"
        android:memtagMode="async"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Kasa">

        <!-- Sistem kod dizini yalnız-okunur; hata ayıklayıcı eklenmesi sürümde kapalı -->
        <meta-data
            android:name="android.webkit.WebView.EnableSafeBrowsing"
            android:value="true" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|uiMode|density|fontScale"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.Kasa">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts" />
        </activity>

        <!-- Otomatik doldurma akışı için kilit açma penceresi -->
        <activity
            android:name=".autofill.AutofillUnlockActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/Theme.Kasa.Transparent" />

        <!-- ═══ Otomatik doldurma servisi ═══ -->
        <service
            android:name=".autofill.KasaAutofillService"
            android:exported="true"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_AUTOFILL_SERVICE">
            <intent-filter>
                <action android:name="android.service.autofill.AutofillService" />
            </intent-filter>
            <meta-data
                android:name="android.autofill"
                android:resource="@xml/autofill_service" />
        </service>

        <!-- Passkey akışının kullanıcı arayüzü (Credential Manager onu başlatır) -->
        <activity
            android:name=".passkey.PasskeyActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/Theme.Kasa.Transparent" />

        <!-- ═══ Passkey sağlayıcısı (Credential Manager) ═══ -->
        <service
            android:name=".passkey.KasaCredentialProviderService"
            android:exported="true"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_CREDENTIAL_PROVIDER_SERVICE">
            <intent-filter>
                <action android:name="android.service.credentials.CredentialProviderService" />
            </intent-filter>
            <meta-data
                android:name="android.credentials.provider"
                android:resource="@xml/credential_provider" />
        </service>

        <!-- ═══ Hızlı ayarlar döşemesi ═══ -->
        <service
            android:name=".tile.VaultTileService"
            android:exported="true"
            android:icon="@drawable/ic_shield"
            android:label="@string/tile_label"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
            <meta-data
                android:name="android.service.quicksettings.ACTIVE_TILE"
                android:value="true" />
        </service>

        <!-- ═══ Ana ekran araç birimi: hızlı eylemler ═══ -->
        <receiver
            android:name=".widget.KasaWidgetProvider"
            android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/kasa_widget_info" />
        </receiver>

        <!-- Pano temizleme zamanlayıcısı -->
        <receiver
            android:name=".core.security.ClipboardClearReceiver"
            android:exported="false" />

        <!-- Varsayılan WorkManager başlatıcısı devre dışı: kendi yapılandırmamızı kullanıyoruz -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>
    </application>
</manifest>
